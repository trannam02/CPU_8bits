; DEFINE REGISTER 
register  8 a,b,c,d
register  8 tl,th
register 16 si,di
register 16 tx
register 16 pc
register 16 ra
register 16 sp

; DEFINE INSTRUCTION
nop
movi a,#1
mov a,b
add a,b,c

; DEFINE INSTRUCTION STRUCTURE
OP   Func   des   rs    rt    const
00 0000000 00000 00000 00000 00000000

